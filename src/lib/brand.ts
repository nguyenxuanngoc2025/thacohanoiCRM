/**
 * Màu thương hiệu THACO AUTO — nguồn DUY NHẤT cho JavaScript.
 *
 * Dùng cho giá trị màu trong JS (vd recharts fill/stroke, inline style qua biến).
 * CSS/Tailwind dùng token `--color-brand` trong `globals.css` (@theme) —
 * hai nơi này PHẢI cùng giá trị. Đổi màu thương hiệu → sửa cả 2.
 */
export const BRAND = '#004b9b';
